#Cancelled automatic pull because wget don't work
#sudo service puppet stop
#sudo /opt/puppetlabs/bin/puppet agent --daemonize --verbose --server=default.c.graceful-matter-161422.internal --runinterval=0